{
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "web-ext": "^8.3.0"
  },
  "scripts": {
    "format": "biome format --write",
    "lint": "biome lint --write",
    "run": "web-ext run -u https://zenn.dev",
    "build": "web-ext build"
  }
}
